name: credit_curve_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - numpy
  - scipy
  - pandas
  - matplotlib
  - pymc
  - arviz
  - jupyter
  - notebook
  - ipykernel
  - pip
  - pip:
      - streamlit
